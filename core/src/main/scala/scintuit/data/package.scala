package scintuit

package object data {
  type AccountId = Long
  type InstitutionId = Long
  type LoginId = Long

  type ChallengeSessionId = Long
  type ChallengeNodeId = Long

  type Base64Binary = String
}
